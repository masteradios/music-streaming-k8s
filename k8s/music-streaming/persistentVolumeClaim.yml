kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: songs-pvc
spec:
  volumeName: songs-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage